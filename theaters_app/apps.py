"""App configuration for the theater application."""

from django.apps import AppConfig


class TheatersAppConfig(AppConfig):
    """App configuration for the theater application."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'theaters_app'
